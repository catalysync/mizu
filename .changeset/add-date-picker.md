---
'@aspect/react': minor
'@aspect/css': minor
---

add DatePicker, DateRangePicker, and Calendar components. Zero date-library dependencies — uses native `Date` + `Intl.DateTimeFormat` with an ISO string contract for value serialization. Popover-based trigger via Radix Popover, min/max bounds, locale/weekStartsOn config, and Field context integration.
