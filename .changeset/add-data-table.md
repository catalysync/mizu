---
'@aspect/react': minor
'@aspect/css': minor
---

add DataTable component with sortable columns, row selection (checkbox + select-all), expandable rows, zebra striping, density modes, loading skeletons, empty state, sticky header, and column alignment. Typed column definitions with generic `<T>` for full type safety. The #1 gap vs sage-carbon's FlatTable.
