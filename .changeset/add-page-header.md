---
'@aspect/react': minor
'@aspect/css': minor
---

add PageHeader pattern — top-of-page preamble with breadcrumbs + title + description + actions + optional toolbar row. The canonical "page preamble" for standalone pages that aren't a specific resource.
