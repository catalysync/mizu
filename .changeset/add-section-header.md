---
'@aspect/react': minor
'@aspect/css': minor
---

add SectionHeader pattern — lightweight section-level header (title + description + actions) for delineating logical regions inside a page. Lives between bare `Heading` and full `ResourceHeader`.
