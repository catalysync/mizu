---
'@aspect/react': minor
'@aspect/css': minor
---

add Form component — semantic `<form>` wrapper with title/description header, stack or grid layout, optional error summary (role="alert"), actions footer with start/end/between alignment, and global `disabled` state that propagates to all nested controls via a hidden fieldset wrapper.
