export const metadata = { title: 'Stack — mizu' };

# Stack

A layout primitive from the `@aspect/react` package.

## Import

```tsx
import { Stack } from '@aspect/react';
```

## CSS class

```css
.mizu-stack {
  /* ... */
}
```

## Usage

```tsx
<Stack gap="1rem">
  <div>Child 1</div>
  <div>Child 2</div>
</Stack>
```

## Props

| Prop        | Type     | Default       | Description        |
| ----------- | -------- | ------------- | ------------------ |
| `gap`       | `string` | Token default | CSS gap value      |
| `className` | `string` | —             | Additional classes |

See the [Storybook](https://mizu-storybook.vercel.app) for interactive examples.
